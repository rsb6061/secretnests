{
  "name": "secretnests",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply secretnests --remote",
    "db:migrate:local": "wrangler d1 migrations apply secretnests --local",
    "seed:hotels": "node scripts/build-legacy-hotel-seed.mjs",
    "seed:reddit": "node scripts/build-reddit-evidence-seed.mjs",
    "audit:secrets": "node scripts/audit-no-secrets.mjs",
    "check": "node --check src/index.js && node --check scripts/build-legacy-hotel-seed.mjs && node --check scripts/build-reddit-evidence-seed.mjs && node --check scripts/audit-no-secrets.mjs && node --check scripts/audit-migration.mjs && npm run audit:secrets && npm run audit:migration && npm run audit:media",
    "audit:migration": "node scripts/audit-migration.mjs",
    "audit:media": "node scripts/audit-media-policy.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
